"""Parse the config and the channels list."""
import configparser


def get_config(config_file):
    """#TODO Parse a config file."""
    return {"max_videos_count": 5}


def get_following_channels(channels_file):
    """#TODO Parse the following channels' list from a file."""
    ...
